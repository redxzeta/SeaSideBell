import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#263238" },
  secondary: { main: "#4DB6AC", contrastText: "#000000" }
};
const themeName = "Outer Space Fountain Blue Caracara";

export default createMuiTheme({ palette, themeName });
